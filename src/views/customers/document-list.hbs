<div class="col-md-10 container-fluid pt-2 pb-2" style="height:95vh;overflow-y:scroll;">
    <div class="row">
        {{#each customers}}
        <div class="col-md-12" margin="auto">
            <div class="card text-center">
                <div class="card-body">
                    {{> list }}
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>There are not customers saved yet.</p>
                <a href="/admin/customers/add">Create One!</a>
            </div>
        </div>
        {{/each}}
    </div>
    </li>
    <div class="container text-center" style="height:15vh">
        <a class="btn btn-lg btn-danger m-3" href="/logout">Cerrar Sesión</a>
    </div>
</div>